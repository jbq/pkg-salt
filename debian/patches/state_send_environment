Description: Send environment with states
Origin: https://github.com/saltstack/salt/commit/113adcd
Author: Bret Palsson <bretep@gmail.com>

--- a/salt/state.py
+++ b/salt/state.py
@@ -530,7 +530,8 @@ class State(object):
         pillar = salt.pillar.get_pillar(
                 self.opts,
                 self.opts['grains'],
-                self.opts['id']
+                self.opts['id'],
+                self.opts['environment'],
                 )
         ret = pillar.compile_pillar()
         if self._pillar_override and isinstance(self._pillar_override, dict):
